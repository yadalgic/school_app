/* class LoadData {
  final int userId;
  final int id;
  final String title;

  LoadData({required this.userId, required this.id, required this.title});

  factory LoadData.fromJson(Map<String, dynamic> json) {
    return LoadData(
        userId: json['userId'], id: json['id'], title: json['title']);
  }
}
 */