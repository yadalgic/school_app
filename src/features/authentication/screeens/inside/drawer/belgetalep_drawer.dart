import 'package:flutter/material.dart';

class BelgeTalep extends StatelessWidget {
  const BelgeTalep({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Belge Talep"),
        backgroundColor: Colors.red.shade900,
      ),
    );
  }
}
